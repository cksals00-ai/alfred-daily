# 아침뉴스 재현본 파이프라인

기준 문서 확인: 이 문서(2026-08-03 신설)

## 이게 뭔가

alfred 가 매일 받는 「간추린 아침뉴스」를 자동으로 재현해 보고, 그가 붙여넣은
**원문과 대조해 점수를 매기고, 그 점수로 다음 날 수집 규칙을 고치는** 루프다.

```
digest.py  수집·편집 → app/public/digest.json   (재현본, 날짜별)
              ↕ 대조
alfred 붙여넣기 → app/public/news.json          (원문, src:"원문" 로 표시)
              ↓
score.py   채점 → rubric.json                   (재현율·정확도·누락 카테고리 누적)
              ↓
           digest.py 의 SLOTS / SEEDS / GUARD 를 고친다
```

## 왜 레포 안에 있나

예약작업은 매번 **새 컨테이너**에서 레포를 클론해 돌린다. 이전에는 이 스크립트가
`/home/claude/work/crawler/` 에만 있어서 예약 실행 때 없는 파일이었고, 그래서
2026-08-01 부터 재현본이 웹검색 수기 수집으로 대체되며 꼭지 수가 16 → 9 → 5 로
떨어졌다. 레포 안에 두면 클론만으로 항상 존재한다.

자격증명은 레포에 넣지 않는다(공개 저장소다). `_paths.cred()` 가 환경변수 →
작업폴더 `.naver.json` 순으로 찾고, 둘 다 없으면 **추정하지 않고 예외를 낸다.**

## 채점 규칙

- `src == "원문"` 인 날만 채점한다. 재현본을 news.json 에 그대로 써 넣은 날을
  채점하면 자기 자신과 비교해 재현율이 100% 로 나온다 — 측정이 아니라 착시다.
- **재현율** = 원문 꼭지 중 재현본이 잡은 비율 (놓친 사건을 본다)
- **정확도** = 재현본 꼭지 중 원문에도 있던 비율 (헛다리를 본다)
- 매칭은 어간 기준 핵심어 겹침이다. 한국어 조사 때문에 `여객선` 과 `여객선에`
  가 다른 토큰이 되므로 뒤 한두 글자를 떼어 함께 비교한다.
- 매칭은 완벽하지 않다. **같은 주제·다른 각도**(예: 원문 "구마모토 반도체 공장
  재가동" ↔ 재현본 "구마모토 강진 수색 종료")는 누락으로 잡힌다. 점수는 절대값
  보다 **추세**로 읽는다.

## 지금까지 확인된 것 (2026-08-03 기준, 정본 1일)

재현율 12% · 정확도 40%. 누락 카테고리는 정치·국회 4, 사건·사고 4, 국제·안보 3,
경제·시장 2, 부동산 1, 날씨·기후 1.

원문과 재현본의 성격 차이가 구조적으로 드러난 부분:

1. **원문은 "오늘 무엇이 예정돼 있나"를 쓴다.** 재현본은 어제 보도량이 많았던
   기사를 쓴다. 원문 1번 꼭지가 "귀국 직후 점검 회의를 엽니다"인데 재현본은
   "귀국했다"를 잡는 식이다. → `sort=date` 만으로는 예고성 기사를 못 잡는다.
2. **원문은 생활·사건·사고를 많이 쓴다.** 신림동 상가 화재, 군산 방사선사,
   차은우 조세심판 — 보도량은 적지만 사람들이 읽는 것들이다. 재현본의 슬롯
   배정(정치 5)이 실제 분포와 다르다.
3. **원문은 숫자를 꼭 넣는다.** "온열질환자 96명 늘어 누적 1,889명, 사망 14명".
   재현본은 제목·요약을 그대로 옮겨 숫자가 빠진다.
4. **원문은 두 문장으로 재작성한다.** 재현본은 네이버 요약문을 그대로 쓴다.

## 개선 백로그 (근거가 쌓이는 대로 반영)

- [ ] 슬롯 배정을 정본 누적 분포로 재산출 (현재 SLOTS 는 348일치 역산값이지만
      카테고리 분류가 거칠다)
- [ ] 사건·사고/사회·생활 씨앗어 확장 — 화재·사망 중심에서 생활 사건으로
- [ ] 예고성 기사 가중 — "오늘", "예정", "열린다" 가 제목에 있는 기사 가산
- [ ] 숫자 보존 — desc 에서 수치 표현을 우선 남기는 재작성 규칙
- [ ] 두 문장 재작성 (현재는 제목+요약 그대로 붙임)
- [ ] **국제·안보 누락 최다 부상** (2026-08-15 채점 근거) — 재기준(8/10) 이후 정본 7일 누적 누락 1위가
      국제·안보 17건 (2위 날씨·기후 16 · 3위 정치·국회 13). 이란·호르무즈, 일본 정치 등 연속 대형
      국제 이슈 국면에서 슬롯 3 고정이 못 따라간다. 손실 분해도 배분 44% > 선택 19% 로 슬롯 쪽이 크다.
- [ ] **단일 대형 이슈 날의 슬롯 탄력 배분** (2026-08-04 근거) — 원문 13꼭지 중 6꼭지가
      폭염이었다(중대경보·상권·온열질환자·정전·경남 40도·열대야 13일째). 재현본은 날씨
      슬롯이 고정 1~2개라 6개 중 2개만 잡았다. 특정 카테고리 기사량이 임계를 넘는 날은
      그 슬롯을 늘리고 다른 슬롯을 줄이는 규칙이 필요하다. 고정 배분은 평시엔 맞지만
      이런 날엔 구조적으로 틀린다.
- [ ] 사건·사고·정치 누락 집중 (정본 2일 누적: 사건·사고 6 · 정치·국회 5 · 국제·안보 4) —
      다만 8/3 은 구 파이프라인 5꼭지 재현본이라 표본 오염. 정본 3일+ 쌓인 뒤 재평가.
- [ ] 정본 3일(8/3~8/5) 누적 — 날씨·기후 10 > 정치·국회 6 = 국제·안보 6 = 사건·사고 6.
- [ ] 정본 4일(8/3~8/6) 누적 — 날씨·기후 14 > 정치·국회 8 > 국제·안보 7 > 사건·사고 6.
      날씨 쏠림 지속. 8/7 재현본부터 탄력 배분(날씨 슬롯 1→5)이 실제 발동했으므로
      다음 정본 채점에서 날씨 누락이 줄었는지로 규칙 효과를 검증할 것.
      날씨 누락은 8/5 승인된 탄력 배분이 8/6부터 발동돼 완화 예상 — 발동 후 3일 지표로 재평가.
      정치·국회는 씨앗어(전당대회·특검·거부권 계열) 보강 후보 (SLOTS/SEEDS 수정은 alfred 승인 후).
- [ ] 정본 6일(8/3~8/6·8/12·8/13) 누적 — 날씨·기후 14 > 국제·안보 13 > 사건·사고 10 > 정치·국회 9.
      국제·안보 누락이 4일 시점 7 → 6일 시점 13으로 가장 가파르게 늘었다(8/12·8/13 이틀에 +6).
      국제·안보 슬롯(현 3)·씨앗어 보강 후보 — SLOTS/SEEDS 수정은 alfred 승인 후.
- [x] **기증 순서를 실측으로 뒤집음** (2026-08-06) — 옛 순서 `문화·스포츠 → 사회·생활 → 부동산`
      은 8/6 에 부동산을 1→0 으로 깎았다. 그런데 정본 4일 기준 부동산은 **4/4일 등장(5꼭지)**,
      사회·생활은 **0/4일(0꼭지)** 이다. 매일 나오는 축을 한 번도 안 나온 축을 지키려고
      희생하고 있었다. 새 순서 `사회·생활 → 문화·스포츠 → 사건·사고`, 사건·사고 바닥 1,
      부동산은 기증자에서 제외.
- [x] **날씨 하위 정원(SUBQUOTA)** (2026-08-06) — 탄력 배분으로 날씨 슬롯이 5가 되자
      39도·40도·폭염특보·폭염중대경보·이슈종합으로 채워졌다. 사실상 한 꼭지의 다섯 판본이다.
      원문의 날씨 8꼭지 중 기온 예보는 2꼭지뿐이고 나머지는 2차 피해(당근값·조류경보·
      프로야구 취소·산불·정전)였다. 기온·특보형 제목을 2개로 제한하고 2차 피해 씨앗을 추가.
- [x] **칼럼·사설 제외** (2026-08-06) — 「[윤준의 부동산수첩]」이 부동산 슬롯을 먹었다.
      아침뉴스 원문은 사건만 싣는다. 필자 이름을 단 브래킷을 표식으로 걸러낸다.
- [x] **채점기 재기준 — 어간 절단 문턱 5자 → 4자** (2026-08-10) — 원문 「…추진 상황을
      점검하기 위해 2차 민관 합동 회의」와 재현본 「李대통령, 3대 메가프로젝트 2차 점검」이
      **겹치는 핵심어 0개**로 떨어졌다. 「점검하기」(4자)에서 두 글자를 떼는 규칙이 5자 이상에만
      걸려 「점검」이 안 나왔기 때문이다. 같은 사건을 누락 + 오탐으로 두 번 세고 있었다.
      문턱을 4자로 낮추니 정본 4일 누적이 **재현율 35→40% · 정확도 41→46%**.
      **수집기가 좋아진 게 아니라 자가 고쳐진 것이다.** 8/10 이전·이후 숫자를 같은 축에 놓지 않는다.
      (새로 붙은 짝 3건은 눈으로 확인했다 — 코스피 반등 0.40, 부동산 세제 개편안 0.40,
      물안경 유해물질 0.50. 허위 매칭이 아니다.)
- [x] **탄력 배분에 기준 정본 유효기간을 뒀다** (2026-08-10, `ANCHOR_MAX_AGE = 2`) —
      8/10 은 직전 정본이 **나흘 전인 8/6** 이라 그날 분포(날씨 50%)로 날씨 슬롯을 5로 올리고
      사회·생활·문화·스포츠를 0 으로 깎았다. 그런데 8/10 원문 16꼭지 중 **6꼭지가 바로 그 두 칸**
      (사설 구급차·밥상물가·아이키/박정민·블랙핑크·블랙이글스)이었다. 어제 신호는 오늘 쓸 수 있어도
      나흘 전 신호는 잡음이다. 기준 정본이 2일보다 오래되면 기본 배분으로 돌아간다.
      **다만 같은 풀 A/B 에서 점수는 거의 안 움직였다(25%/24% → 25%/25%).**
      슬롯을 열어줘도 그 카테고리 후보 자체가 안 잡힌다는 뜻 — 다음 병목은 배분이 아니라 **씨앗어**다.
- [ ] **사회·생활·문화·스포츠 씨앗어 부족** (2026-08-10 근거) — 슬롯을 1씩 줘도 아이키·박정민
      일본 방문, 블랙핑크 10주년, 블랙이글스 60년, 사설 구급차 사망이 후보에 아예 안 올라왔다.
      연예·군·의료사고 계열 씨앗이 없다. 정본이 더 쌓이면 이 두 카테고리 씨앗을 먼저 손본다.
- [ ] **크롤링 시각 민감도** (2026-08-06 관측) — 같은 날 07:00 정시 실행은 50%/47%,
      같은 코드로 08:20 에 재실행하니 25~31% 로 떨어졌다. `sort=date` 라 한 시간 뒤의
      기사 풀은 이미 다른 풀이다. **재현본 품질 비교는 반드시 같은 기사 풀 위에서 해야 한다**
      (A/B 할 때 `search()` 를 캐싱해 두 설정을 같은 풀로 돌린다). 07:00 고정 유지가 중요하다.

- [x] **손실 분해 지표 도입 — 「구조 한계」** (2026-08-14, `score.py`) — 재현율이 낮을 때
      원인이 「무엇을 골랐나」인지 「몇 칸을 줬나」인지 구분이 안 됐다. 그래서 각 칸 안에서
      완벽히 골랐다고 가정한 적중 상한(원문 분포 ∩ 재현본 분포)을 같이 낸다.
      **정본 7일 실측: 실제 재현율 37% · 구조 한계 56%.**
      → 배분(슬롯)에서 44%p, 칸 안 선택에서 19%p 를 잃는다. **병목은 배분이 선택의 두 배다.**
      8/10~8/13 에 한 작업(씨앗 보강·기증자 바닥)은 전부 작은 쪽 절반을 손본 것이었다.
- [x] **슬롯 재산출을 히스토리로 하는 안 — 백테스트 결과 기각** (2026-08-14) — 검증된 정본
      9일 실측 분포는 현행 SLOTS 와 크게 다르다(날씨·기후 평균 4.1 인데 슬롯 1, 정치·국회
      평균 2.6 인데 슬롯 5). 그래서 「지난 N일 정본 평균으로 슬롯을 다시 뽑는」 안을
      미래를 안 보는 방식으로 백테스트했다. **구조 한계 60% → 롤링7일 61% · 롤링14일 61%.**
      거의 안 움직인다. 이유는 분산이다 — 날씨·기후 일별 꼭지가 3·6·8·8·3·3·2·1·3 으로
      흔들려 평균이 맞아도 그날이 안 맞는다. **카테고리 구성은 과거로부터 예측되지 않는다.**
      어제 원문(탄력 배분)도, N일 평균(롤링)도 같은 이유로 약하다.
      → 배분을 고치려면 히스토리가 아니라 **그날 아침 후보 자체의 카테고리별 밀도**를 봐야 한다.
      이게 다음 과제다. (같은 이유로 SLOTS 하드코딩 교체는 하지 않았다. 9일은 표본이 작고
      8월은 폭염철이라 날씨가 부풀려져 있을 수 있다 — 계절 편향을 아직 못 걷어냈다.)
- [x] **사회·생활·문화·스포츠 씨앗 보강** (2026-08-13) — 실업률·국세청 세무조사·연예계 논란·
      아이돌·축구협회 6개 추가, 가드 4곳 확장(실업/체납/아이돌·K팝/녹조/사관학교).
      넣기 전에 그날 원문이 실은 사건이 실제 후보로 올라오는지 8건 중 7건 확인했다.
      **당일 점수는 안 올랐다** — 슬롯이 1이면 후보만 늘고 뽑히는 건 하나인데, 순위가
      「그날의 사건」이 아니라 「보도량 많은 기사」를 고른다. 다만 8/14 정시 실행에서
      국세청 세무조사 씨앗이 사회·생활 칸을 실제로 물어왔다. 호출량 37 → 42콜.
- [x] **기증자 바닥을 전부 1 로** (2026-08-13) — 8/13 탄력 배분이 사회·생활·문화·스포츠를
      0 으로 깎았는데 그날 원문이 그 두 칸에 5꼭지를 실었다. 슬롯 0 이면 적중률도 0 이다.

## 신선도 점검 (freshness.py)

score.py 가 "재현본이 얼마나 정확한가"를 본다면, freshness.py 는 그 앞의 질문 —
**"애초에 갱신이 돌긴 했는가"** — 를 본다. 둘은 다른 실패다. digest.py 가 컨테이너에서
사라진 채 몇 주를 돌았을 때 화면은 멀쩡했다. 어제 데이터가 남아 있으면 오늘 것처럼
읽히기 때문이다. 그 침묵을 깨는 게 이 스크립트다.

12개 소스의 **최신 날짜만** 읽는다. 담당 예약작업을 함께 찍어서, 멈춘 축이 있으면
어느 작업을 봐야 하는지가 바로 나온다.

| 축 | 담당 예약작업 | 임계(정상/주의) |
|---|---|---|
| news.json · digest.json | Alfred Daily 07:00 `trig_01QSnb9cSoQyNvLku62aKCA4` | 1일 / 2일 |
| insta.json | 같은 작업 §0 (교신함 수급) | 1일 / 2일 |
| GS_DAILY · INSIGHT_DAILY | GS·인사이트 09:00 `trig_01QM65z2VTBqfM4AB5Zgd9dD` | 1영업일 / 2영업일 |
| GS_PERF.asOf | 같은 작업 §1-b | 2영업일 / 3영업일 |
| GS_PERF.fcstAsOf | 같은 작업 (주간 스냅샷) | 7일 / 10일 |
| TREND_PLAN | 같은 작업 §3 (일요일만) | 7일 / 9일 |
| WEEKLY_BRIEF · NEWS_WEEKLY | 주간 브리핑 일 07:00 `trig_01NqusWzxt6ikGFZkKwRJ15C` | 7일 / 9일 |
| NOTES · TODAY_AGENDA | 없음 — 손으로 채운다 | 수동(참고만) |

설계상 지켜야 할 것 세 가지.

1. **GS 계열은 달력일이 아니라 영업일로 센다.** 출처 페이지가 주말·공휴일에 갱신되지
   않기 때문이다. 토요일마다 "하루 밀렸다"고 울리는 경보는 곧 아무도 안 본다.
2. **GS_PERF.asOf 는 임계를 한 칸 밀어 놓았다.** 출처가 전일 실적을 싣는 구조라
   1영업일 지연이 정상이다. 구조적 지연을 고장으로 부르지 않는다.
3. **수동 축은 실패로 세지 않는다.** 롱블랙 노트는 alfred 가 링크를 보낼 때만 늘어난다.
   공백은 고장이 아니라 그냥 공백이다. 경과일은 보여주되 판정은 「수동」으로 남긴다.

파일·상수를 못 읽으면 「조회실패 + 사유」로 남기고 넘어간다. 0으로도, 추정치로도
채우지 않는다.

## 사용법

```bash
python3 tools/crawler/digest.py            # 오늘치 재현본 생성 + digest.json 갱신
python3 tools/crawler/score.py 2026-08-03  # 하루 채점
python3 tools/crawler/score.py --all       # 정본 전체 누적 + rubric.json 기록
python3 tools/crawler/freshness.py         # 12개 소스 신선도 표 + freshness.json 기록
python3 tools/crawler/freshness.py --line  # 보고용 한 줄만
python3 tools/crawler/apiusage.py 레이더 84 # 네이버 검색 API 실제 호출 수 기록
```

## 커밋 작성자 (gitid.py)

예약작업 프롬프트에는 「커밋 전에 `git config user.email noreply@anthropic.com` 을 걸어라」가
적혀 있다. 그런데 지시는 지켜질 때도 있고 아닐 때도 있었다 — 2026-07-31(b3af2b8)과
2026-08-09(95a3155) 두 번은 누락돼 `Higgsfield Agent <agent@higgsfield.ai>` 로 커밋이
올라갔고 GitHub 에서 **Unverified** 로 표시됐다.

그래서 지시를 더 크게 적는 대신 **실행되게** 만들었다. `tools/gitid.py` 의 `ensure()` 가
레포의 user.email/user.name 을 맞추고, 예약작업이 반드시 부르는 두 스크립트가 시작할 때
이걸 호출한다.

| 작업 | 부르는 스크립트 | 훅 위치 |
|---|---|---|
| 07:00 재현본 | `tools/crawler/digest.py` | `_paths` import 직후 |
| 09:00 GS·인사이트 | `tools/gs_scrape.py` | 최상단 import 직후 |
| 일 07:00 주간 브리핑 | (레포 스크립트를 안 쓴다) | 프롬프트 §0 에 명시 |

git 이 없거나 레포가 아니면 조용히 넘어간다 — 이 훅 때문에 수집이 죽으면 안 된다.
이미 올바르게 설정돼 있으면 아무것도 하지 않는다.

## 호출량 기록 (apiusage.py)

화면의 「무료 한도 대비 호출량」은 2026-08-07 까지 손으로 박아둔 상수 300 이었다.
한 번도 안 변했고, 매일 도는 재현본 수집(씨앗어 37개 = 37콜)은 세어지지도 않았다.
설계치를 미터 모양으로 그려 놓으면 보는 사람은 그걸 실측으로 읽는다.

지금은 `digest.py` 의 `search()` 가 호출을 세고, 실행 끝에 `apiusage.record("재현본", n)`
으로 `app/public/apiusage.json` 에 적는다. 주간 레이더는 예약작업 프롬프트를 고치는
대신 `apiusage.sync_radar()` 가 `dashboard.html` 의 `TREND_PLAN.calls` / `updated` 를
읽어 옮긴다 — 레이더는 원래부터 그 두 값을 쓰고 있었으므로 새로 시킬 일이 없다.
07:00 실행이 매일 한 번씩 이 동기화를 부른다. 같은 (날짜, 작업) 기록은 덮어쓴다 —
재실행이 누적을 부풀리면 안 된다.

기록이 없는 날은 **0으로 채우지 않는다.** 0 은 「측정된 0」을 뜻한다. 화면도
「기록 없음」으로 남기고 실측 시작일을 같이 보여준다.
